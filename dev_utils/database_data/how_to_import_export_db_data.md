# how to import export db data

db data con be exported and imported by using the django import export tool, in django section, in the detail of the model you want to import/export.

The data should be imported according to the model dependencies, in terms of ForeignKeys.

