# cron per l'app aqi_luftdaten

# sudo vim /etc/cron.d/aqi_luftdaten-cron

#salva i valori nel modello storico orario
#arrangia le serie orarie attingendo al modello storico orario
0 * * * * root cd /var/www/aqi_luftdaten;source venv/bin/activate;python manage.py save_current_pm_values_in_history

# test
# * * * * * root cd /home/pi/;touch hello_time.txt;echo "hello the time is $(date)" >> hello_time.txt